module.exports = {
    presets: [
      // Transforma o código ES6+ em JavaScript compatível com navegadores mais antigos
      "@babel/preset-env",
  
      // Suporte ao JSX e recursos do React
      "@babel/preset-react"
    ],
    plugins: [
      // Outros plugins que você pode adicionar futuramente, se necessário
    ],
  };
  